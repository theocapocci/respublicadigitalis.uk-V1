---
// src/pages/republic/index.astro
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

const allNotes = await getCollection('notes');

// Updated to filter by 'publish' and sort by 'date'
const publishedNotes = allNotes.filter(note => note.data.publish === true);

---
<BaseLayout frontmatter={{ title: 'Notes | Respublica Digitalis' }}>
  <div class="main-content-container">
    <section class="base-content">
      <h1>Notes</h1>
      <p>Here is a collection of notes.</p>
    </section> 

    <div class="gallery-grid">
      {publishedNotes.map(note => (
        <a href={`/republic/notes/${note.slug}`} class="gallery-item">
          <div class="card">
            {note.data.image && (
              <img src={note.data.image} alt={`Cover for ${note.data.title}`} class="card-image" />
            )}
            <div class="card-content">
              <h3 class="card-title">{note.data.title}</h3>
              {/* Updated to use 'description' and checks if it exists */}
              {note.data.description && (
                <p class="card-excerpt">{note.data.description}</p>
              )}
            </div>
          </div>
        </a>
      ))}
    </div>
  </div>
</BaseLayout>

<style>
  /* --- PAGE-SPECIFIC LAYOUT AND CONTENT STYLES --- */
  .main-content-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 20px;
  }

  .base-content {
    padding: 20px 0;
    text-align: left;
    max-width: 1100px; 
    margin: 0 auto;
  }
 
  .base-content h1 {
    font-family: 'Cormorant SC', serif;
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 1rem;
  }

  .base-content p {
    font-family: 'Sorts Mill Goudy', serif;
    font-size: 1.3em;
    line-height: 1.8;
    margin: 20px 0 30px 0;
  }

  /* --- GALLERY STYLES --- */
  .gallery-grid {
    max-width: 1100px;
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 1.5rem;
    margin: 0 auto;
  }

  .gallery-item {
    text-decoration: none;
    color: inherit;
    transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
  }

  .gallery-item:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 16px rgba(0,0,0,0.08);
  }

  .card {
    background-color: #ffffff;
    border: 1px solid #E8E5DF;
    border-radius: 4px;
    height: 100%;
    display: flex;
    flex-direction: column;
    overflow: hidden;
  }

  .card-image {
    width: 100%;
    height: 200px;
    object-fit: cover;
    border-bottom: 1px solid #E8E5DF;
  }

  .card-content {
    padding: 1.5rem;
  }
  
  .card-title {
    font-family: 'Cormorant SC', serif;
    font-size: 1.7rem;
    font-weight: 600;
    margin: 0 0 1rem 0;
    color: #3A3A3A;
  }
  
  .card-excerpt {
    font-family: 'Sorts Mill Goudy', serif;
    font-size: 1.1rem;
    line-height: 1.6;
    color: #555;
    margin: 0;
  }
</style>